# Application-specific docker-compose for: test-app
# Auto-generated by scripts/generate-app-compose.sh
# DO NOT EDIT MANUALLY - regenerate with: ./scripts/generate-app-compose.sh test-app
#
# Usage from root:
#   docker compose -f docker-compose.base.yml -f applications/test-app/docker-compose.yml up

version: '3.8'

services:
  test-app-backend:
    build:
      context: ./applications/test-app/backend
      dockerfile: Dockerfile
      args:
        BUILD_DATE: ${BUILD_DATE:-2024-01-01T00:00:00Z}
        BUILD_VERSION: ${BUILD_VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        GIT_BRANCH: ${GIT_BRANCH:-main}
    image: test-app-backend:${BUILD_VERSION:-dev}
    container_name: test-app-backend
    ports:
      - "${TEST_APP_BACKEND_PORT:-8000}:8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-appuser}:${POSTGRES_PASSWORD:-apppassword}@database:5432/${POSTGRES_DB:-appdb}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      RATE_LIMIT_ENABLED: ${RATE_LIMIT_ENABLED:-false}
    depends_on:
      database:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
  test-app-frontend:
    build:
      context: ./applications/test-app/frontend
      dockerfile: Dockerfile
      args:
        BUILD_DATE: ${BUILD_DATE:-2024-01-01T00:00:00Z}
        BUILD_VERSION: ${BUILD_VERSION:-dev}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        GIT_BRANCH: ${GIT_BRANCH:-main}
    image: test-app-frontend:${BUILD_VERSION:-dev}
    container_name: test-app-frontend
    ports:
      - "${TEST_APP_FRONTEND_PORT:-3000}:3000"
    environment:
      VITE_BACKEND_URL: ${TEST_APP_BACKEND_URL:-http://test-app-backend:8000}
    depends_on:
      test-app-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - app-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
