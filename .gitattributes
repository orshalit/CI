# Keep line endings consistent across platforms
# (prevents CRLF churn on Windows and keeps CI scripts predictable).

* text=auto

# Source code
*.py text eol=lf

# Workflows / config
*.yml text eol=lf
*.yaml text eol=lf

# Shell scripts
*.sh text eol=lf

# PowerShell scripts
*.ps1 text eol=crlf
